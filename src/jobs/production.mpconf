"""
local jobs
"""
load("jobs.pinc", "populate")
load("helpers/registry.pinc", "JobRegistry")

reg = JobRegistry(
    "production-jobs-executor",
    "jobs/production",
    cluster_address="https://localhost:4646",
)
populate(reg)


def main():
    return reg.output
